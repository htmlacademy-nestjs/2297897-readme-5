export const JWT_ACCESS_HEADER = {
  name: 'Auth',
  description: 'Access JWT token',
  required: true,
}

export const JWT_REFRESH_HEADER = {
  name: 'Auth',
  description: 'Refresh JWT token',
  required: true,
}
